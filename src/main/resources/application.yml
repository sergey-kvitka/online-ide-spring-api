server:
  port: 8800
  servlet:
    context-path: /ide/api

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/online-db-projects
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: false

jwt:
  token:
    expired: 864000000
    secret: secret

